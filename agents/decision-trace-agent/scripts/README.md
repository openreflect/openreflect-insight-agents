# Decision Trace Scripts

No deterministic scripts are included yet.

Future scripts should be paired with prompts and should validate trace outputs against `schemas/insight-record.schema.json` or a future decision-trace schema.

